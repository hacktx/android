<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_width="125dp"
        android:layout_height="125dp"
        android:id="@+id/cloud"
        android:src="@drawable/cloud"
        android:layout_marginTop="70dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="@string/sched_day_empty_title"
        android:id="@+id/fragment_empty_title"
        android:layout_gravity="center_horizontal"
        android:layout_below="@+id/cloud"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/fragment_empty_btn"
        android:id="@+id/scheduleEmptyTryAgain"
        android:textColor="@color/primaryDark"
        style="?android:attr/borderlessButtonStyle"
        android:layout_below="@+id/fragment_empty_title"
        android:layout_centerHorizontal="true" />

</RelativeLayout>